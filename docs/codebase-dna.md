# Codebase DNA

Read by the `reviewer` subagent and by `docs/build-graph/doctor.sh`. For this repository the
codebase DNA — module map, canonical mechanisms, conventions, known traps — is `CLAUDE.md` at the
repository root; keep it there rather than copying it here.

| Repo | Canonical branch | Notes |
| -- | -- | -- |
| this repository | `test` | CI builds `BranchName=test` into the `velents-test` environment |

Last reviewed: 2026-09-10.

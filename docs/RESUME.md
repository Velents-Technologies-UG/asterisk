# Resume note

Session-continuity note (operating rule §11). Overwritten at the end of every `/increment`: current
state, what is verified, what is open, decisions made, rules that must hold, what is next. A fresh
session reads this first; it inherits nothing else.

## Now

**State:** Agentic Build Graph installed (2026-09-10); no increment has run yet.
**Verified:** —
**Open:** —
**Next:** run `/increment <task>` for the next piece of work.

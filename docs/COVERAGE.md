# Coverage

Register of the Agentic Build Graph. What is **verified** (real request → real data, negative case
proven), what is **built but UNVERIFIED** (with exactly what is needed to verify it), and known scope
gaps. Never round `built` up to `verified` here.

| Date | Surface | State (built / verified / shippable / demoable / UNVERIFIED) | Evidence or what is needed | Linear |
| -- | -- | -- | -- | -- |
